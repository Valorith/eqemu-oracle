{
  "mcpServers": {
    "eqemu-oracle": {
      "type": "stdio",
      "command": "./scripts/eqemu_oracle_launcher.cmd",
      "args": [
        "mcp-serve"
      ],
      "cwd": ".",
      "note": "Local stdio MCP server for deterministic EQEmu quest API, schema, and docs lookup with extension overlays."
    }
  }
}
